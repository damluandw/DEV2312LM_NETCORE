@model IEnumerable<DanhGiaRenLuyen_V2.Models.DBModel.Answer>
@{
    ViewData["Title"] = "DetailsAnswer";
    Layout = "~/Areas/Admins/Views/Shared/_LayoutAdmin.cshtml";
    // var answers = Model as List<DanhGiaRenLuyen_V2.Models.DBModel.Answer>;
    var question = @ViewBag.Question as DanhGiaRenLuyen_V2.Models.DBModel.Question;
}

<h1>@question.ContentQuestion</h1>
<p>
    @* <a asp-controller="Answers" asp-action="Create">Thêm câu trả lời</a> *@
    <a asp-action="CreateAnswer" asp-route-id="@question.Id">Thêm câu trả lời</a>
</p>
<table class="table">
    <thead>
        <tr>
          @*   <th>
                @Html.DisplayNameFor(model => model.Question)
            </th> *@
            <th>
                @Html.DisplayNameFor(model => model.ContentAnswer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AnswerScore)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
               @*  <td>
                    @Html.DisplayFor(modelItem => item.Question.ContentQuestion)
                </td> *@
                <td>
                    @Html.DisplayFor(modelItem => item.ContentAnswer)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AnswerScore)
                </td>

                <td>
            @*         <a asp-controller="Answers" asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-controller="Answers" asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-controller="Answers" asp-action="Delete" asp-route-id="@item.Id">Delete</a> *@
                    <a asp-action="EditAnswer" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="DetailsAnswer" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="DeleteAnswer" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
@* 
@foreach (var item in answers)
{
    <div class="row">
        <div class="col-md-4">
            <form asp-action="EditAnswer">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="@item.Id" />
                <div class="form-group hide">
                    <label asp-for="@item.QuestionId" class="control-label"></label> 
                    <select asp-for="@item.QuestionId" class="form-control" asp-items="ViewBag.QuestionId"></select> 
                    <span asp-validation-for="@item.QuestionId" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="@item.ContentAnswer" class="control-label"></label>
                    <input asp-for="@item.ContentAnswer" class="form-control" />
                    <span asp-validation-for="@item.ContentAnswer" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="@item.AnswerScore" class="control-label"></label>
                    <input asp-for="@item.AnswerScore" class="form-control" />
                    <span asp-validation-for="@item.AnswerScore" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Save" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>
}


<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
} *@